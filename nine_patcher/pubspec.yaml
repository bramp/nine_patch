name: nine_patcher
description: A command line tool to creates nine-patch JSON metadata files.
version: 1.0.1
repository: https://github.com/bramp/nine_patch

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  image: ^4.0.17
  json_annotation: ^4.8.1
  nine_patch_common: ^1.0.0
  path: ^1.8.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.0
